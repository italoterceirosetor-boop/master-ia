{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn master_server:app --bind 0.0.0.0:$PORT --timeout 300 --worker-class gthread --workers 2 --threads 4",
    "restartPolicyType": "ON_FAILURE"
  }
}
